---
phase: 01-ui-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Starlit_Stationary_frontend/src/Pages/HomePage.jsx
  - Starlit_Stationary_frontend/src/Components/Navbar.jsx
  - Starlit_Stationary_frontend/src/App.jsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-05
user_setup: []

must_haves:
  truths:
    - "Hero section uses the new color palette (primary navy bg, warm cream accents, secondary blue elements)"
    - "Hero section has a professional, modern layout with clear value proposition"
    - "Featured products carousel matches the new theme"
    - "Loader on the homepage uses useSmartLoader — no flicker for fast loads, minimum display time enforced"
    - "Navbar uses the new color palette"
    - "Page transitions use the new palette consistently"
  artifacts:
    - path: "Starlit_Stationary_frontend/src/Pages/HomePage.jsx"
      provides: "Redesigned homepage with hero section and smart loader"
      min_lines: 300
    - path: "Starlit_Stationary_frontend/src/Components/Navbar.jsx"
      provides: "Navbar with new color palette"
    - path: "Starlit_Stationary_frontend/src/App.jsx"
      provides: "Auth checker loader updated to use new palette"
  key_links:
    - from: "HomePage.jsx"
      to: "src/Hooks/useSmartLoader.js"
      via: "import useSmartLoader for controlling loader visibility"
      pattern: "useSmartLoader"
    - from: "HomePage.jsx"
      to: "src/store/useProductStore.js"
      via: "getFeatureProduct — loading state drives useSmartLoader"
    - from: "Navbar.jsx"
      to: "tailwind.config.js"
      via: "Uses bg-primary, text-warm, etc. from new palette"
      pattern: "primary|warm|secondary|accent"
---

<objective>
Redesign the homepage hero section with a professional, conversion-focused layout using the new color palette, and integrate the smart loader to eliminate flickering.

**Purpose:** The hero section is the first thing visitors see — it needs to communicate trust, quality, and value immediately. The smart loader prevents the jarring flash-of-loader on fast connections.

**Output:**
1. Redesigned hero section on HomePage.jsx using primary/secondary/warm palette
2. Featured products carousel restyled with new colors
3. Smart loader integration on the homepage
4. Navbar updated with new palette colors
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-ui-enhancement/01-01-CONTEXT.md

**Current state references:**
@Starlit_Stationary_frontend/src/Pages/HomePage.jsx (current — 349 lines, dark blue hero with yellow accent)
@Starlit_Stationary_frontend/src/Components/Navbar.jsx (current — 449 lines, blue gradient theme)
@Starlit_Stationary_frontend/src/App.jsx (current — loader uses blue spinner)

**Color Palette Tokens (from Plan 01):**
- `bg-primary`, `text-primary` — Dark Navy #213448
- `bg-secondary`, `text-secondary` — Muted Blue #547792
- `bg-accent`, `text-accent`, `border-accent` — Light Steel Blue #94B4C1
- `bg-warm`, `text-warm`, `border-warm` — Warm Cream #EAE0CF

**Smart Hook (from Plan 01):**
```js
const { showLoader, isLoading } = useSmartLoader({ delay: 300, minDisplay: 500 });
```

**Design decisions (per D-01, D-04, D-05):**
- Hero bg: primary (#213448) with warm (#EAE0CF) and accent (#94B4C1) decorative elements
- CTA buttons: warm bg with primary text
- Text on hero: warm (#EAE0CF) for headings, accent (#94B4C1) for body
- Featured section header: secondary (#547792) gradients
- Product cards: white bg with warm accents
- Navbar: primary bg with warm text
- Original yellow accent → accent (#94B4C1) / warm (#EAE0CF)
- Original blue gradient → primary/secondary gradients
</context>

<interfaces>
<!-- Extracted types and patterns from existing code -->

From src/Hooks/useSmartLoader.js:
```js
export function useSmartLoader({ delay = 300, minDisplay = 500 } = {}) {
  // Returns: { showLoader: boolean, isLoading: boolean }
}
```

From src/store/useProductStore.js:
```js
const { gettingFeatureProduct, getFeatureProduct, featureProduct } = useProduct();
// gettingFeatureProduct: boolean — raw loading state
// featureProduct: array — featured products
```

From src/Components/Navbar.jsx:
```js
// Uses lucide-react icons
// Props: none (uses stores internally: useAuth, useIsAdmin, useCart, useLocation)
// Structure: sticky top-0 nav, desktop links, mobile sidebar
```

From src/App.jsx:
```js
const App = () => {
  const { authUser, checkAuth, isCheckingAuth } = useAuth();
  const { authAdmin, isCheckingAdmin, isAdmin } = useIsAdmin();
  // Loading state: {isCheckingAuth && !authUser} && {isCheckingAdmin && !authAdmin}
  // Currently renders <Loader className="size-10 animate-spin" /> inside centered div
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Redesign hero section with professional layout and new palette</name>
  <files>
    Starlit_Stationary_frontend/src/Pages/HomePage.jsx
  </files>
  <action>
    Rewrite the hero section (lines ~236-310 of HomePage.jsx) with a professional, modern layout:

    1. **Background**: Change from `bg-gradient-to-br from-blue-900 via-blue-700 to-blue-500` to `bg-gradient-to-br from-primary via-secondary to-primary`
    
    2. **Decorative Elements**: Use accent (#94B4C1) and warm (#EAE0CF) for the blur circles instead of blue:
       - Change `bg-white/10` circles → `bg-accent/20` and `bg-warm/10`
       - Add geometric SVG patterns for visual interest (subtle dots or diagonal lines in accent/20)

    3. **Badge/Pill**: Change the "Premium Quality Stationery" badge:
       - bg from `bg-white/20` → `bg-warm/15` or `bg-accent/20`
       - text from `text-white` → `text-warm`
       - border: `border-accent/30`
       - Icon: use `Sparkles` with `text-warm`

    4. **Main Heading**:
       - "Shop the Latest" → keep as `text-warm`
       - "Stationery Collection" gradient: change from `from-yellow-200 to-yellow-400` → `from-warm to-warm-100` (or `from-accent-100 to-warm`)
       - Add a subtitle line about Starlit Stationery's mission/quality
       - Add a stats/numbers row (e.g., "1000+ Happy Customers | 200+ Products | Pan India Delivery")

    5. **CTA Button** ("Shop Now"):
       - bg from `bg-white text-blue-900` → `bg-warm text-primary`
       - hover effect from `from-yellow-400 to-yellow-500` → `from-accent to-warm`
       - keep the arrow animation

    6. **Trust Indicators**: Restyle the three indicators below the CTA:
       - Change green check circles → use accent or warm colored circles
       - Text from `text-white/80` → `text-warm/80`
       - Change checkmark SVGs to use warm color

    7. **Featured Products Section** (lines ~313-342):
       - Section bg from `bg-white/95 backdrop-blur-sm` → `bg-warm/95 backdrop-blur-sm`
       - Header gradient from `from-blue-600 to-blue-700` → `from-secondary to-primary`
       - Sparkle icon from `text-yellow-300` → `text-warm`
       - Subtitle text from `text-blue-100` → `text-accent-100`
       - Carousel area bg from `from-gray-50 to-white` → `from-warm-50 to-warm`

    IMPORTANT — Keep all existing functionality:
    - DotButton component
    - FeaturedProductCarousel component with embla carousel
    - Quantity controls and add-to-cart logic
    - The `useMediaQuery` hook
    - The loading state (will be enhanced in Task 2)
    - PageTransition
  </action>
  <verify>
    <automated>
      1. grep for "primary" in HomePage.jsx — should reference `bg-primary`, `text-primary`, etc.
      2. grep for "warm" in HomePage.jsx — should reference `bg-warm`, `text-warm`, etc.
      3. grep for "from-blue-900\|from-blue-600\|to-blue-500\|to-blue-700" in HomePage.jsx — should NOT find these old classes
      4. `Test-Path -LiteralPath "Starlit_Stationary_frontend/src/Pages/HomePage.jsx"` → True
    </automated>
  </verify>
  <done>
    - Hero section uses primary navy background with warm cream text
    - CTA buttons use warm background with primary text
    - Trust indicators use accent/warm colors instead of green
    - Featured products section uses warm backgrounds with secondary/primary headers
    - All old blue-900/blue-700/blue-500 gradients replaced with primary/secondary
    - All yellow accents replaced with warm/accent
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate smart loader on homepage + enhance loader component</name>
  <files>
    Starlit_Stationary_frontend/src/Pages/HomePage.jsx
    Starlit_Stationary_frontend/src/App.jsx
  </files>
  <action>
    1. In `HomePage.jsx`:
       - Add import: `import { useSmartLoader } from "../Hooks/useSmartLoader.js";`
       - Change the loading check from:
         ```jsx
         if (gettingFeatureProduct || !featureProduct) {
         ```
         To use the smart loader:
         ```jsx
         const { showLoader } = useSmartLoader({ 
           isLoading: gettingFeatureProduct, 
           delay: 300, 
           minDisplay: 500 
         });
         
         if (showLoader || (!featureProduct && gettingFeatureProduct)) {
         ```
       - Update the loader itself:
         - Change bg from `from-blue-50 to-white` → `from-warm to-warm-100`
         - Change spinner color from `text-blue-600` → `text-primary`
         - Add a subtle pulsing animation to the loader container
         - Change text from `text-gray-600` → `text-secondary`

    2. In `App.jsx` (lines ~35-41):
       - Change the auth checking loader from the plain blue spinner:
         - bg from default → `bg-warm`
         - spinner from `text-current` → `text-primary`
         - Add a subtle background: `bg-gradient-to-br from-warm to-warm-100`
         - Wrap in a container that uses the new palette

    3. The loader should not show for fast operations. When `gettingFeatureProduct` completes in <300ms, the loader stays hidden. When it's >300ms, the loader appears and stays visible for at least 500ms after loading completes.
  </action>
  <verify>
    <automated>
      1. grep for "useSmartLoader" in HomePage.jsx — should be imported and used
      2. grep for "from-blue-50 to-white" in HomePage.jsx — should NOT exist (replaced with warm)
      3. grep for "text-blue-600" in the loader section of HomePage.jsx — should use "text-primary"
      4. grep for "text-primary" in App.jsx loader section — should use new palette
    </automated>
  </verify>
  <done>
    - useSmartLoader imported and used in HomePage.jsx
    - Loader bg uses warm palette (not blue)
    - Spinner uses primary color (not blue-600)
    - Fast loads (<300ms) produce no loader flicker
    - App.jsx auth checker uses primary/warm palette
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Navbar with new color palette</name>
  <files>
    Starlit_Stationary_frontend/src/Components/Navbar.jsx
  </files>
  <action>
    Rewrite all color classes in Navbar.jsx to use the new palette:

    **Desktop Navbar (main nav bar):**
    - `nav` bg: from `bg-gradient-to-r from-blue-700 via-blue-600 to-blue-700` → `bg-gradient-to-r from-primary via-primary to-secondary`
    - `nav` border: from `border-blue-800/30` → `border-primary-900/30`
    - Logo icon container: from `bg-white/10 hover:bg-white/20` → `bg-secondary/30 hover:bg-secondary/50`
    - Logo text: keep `text-white` (white works with dark navy)
    - Logo subtitle: from `text-blue-100` → `text-accent`
    
    **Nav Links:**
    - Active link bg: from `bg-white/20` → `bg-secondary/40`
    - Active link text: keep `text-white`
    - Inactive link text: from `text-blue-50 hover:bg-white/10 hover:text-white` → `text-accent hover:bg-secondary/30 hover:text-warm`
    - Active tab indicator: from `bg-yellow-400` → `bg-warm`
    - Divider: from `bg-white/20` → `bg-accent/30`
    
    **Auth Buttons:**
    - Sign Up: from `bg-white/10 text-white hover:bg-white/20` → `bg-warm/20 text-warm hover:bg-warm/30`
    - Sign In: from `bg-white/10 text-white hover:bg-white/20 border border-white/30` → `bg-accent/20 text-warm hover:bg-accent/30 border border-warm/30`
    - Logout: from `text-white hover:bg-red-500/20 border border-red-400/50` → `text-warm hover:bg-red-500/20 border border-red-400/50` (keep red for destructive action)
    
    **Mobile Menu:**
    - Overlay bg: from `bg-black/50` → `bg-primary/80`
    - Sidebar bg: from `from-blue-800 via-blue-700 to-blue-600` → `from-primary via-primary to-secondary`
    - Section labels: from `text-blue-200` → `text-accent`
    - Mobile link active: from `bg-white/20` → `bg-secondary/40`
    - Mobile link inactive: from `text-blue-50 hover:bg-white/10` → `text-accent hover:bg-secondary/30`
    
    **Admin Dropdown:**
    - Dropdown bg: keep `bg-white`
    - Active item: from `bg-blue-100 text-blue-700` → `bg-accent/20 text-primary`
    - Inactive item: from `text-gray-700 hover:bg-blue-50` → `text-gray-700 hover:bg-accent/20`

    Keep all functionality, imports, and component structure identical. Only change color classes.
  </action>
  <verify>
    <automated>
      1. grep for "from-blue-700 via-blue-600" in Navbar.jsx — should NOT find these (replaced with primary)
      2. grep for "blue-100\|blue-200\|blue-50" in Navbar.jsx — should NOT find these color references
      3. grep for "primary\|warm\|secondary\|accent" in Navbar.jsx — should find new palette classes
      4. grep for "useState\|useEffect\|import" in Navbar.jsx — all imports preserved
    </automated>
  </verify>
  <done>
    - Navbar bg uses primary/secondary gradient (not blue)
    - Nav links use accent/warm for text, secondary/primary backgrounds
    - Active tab indicator uses warm (not yellow)
    - Mobile menu uses primary/secondary palette
    - Admin dropdown uses accent for active states
  </done>
</task>

</tasks>

<verification>
1. Homepage hero renders with primary (#213448) as dominant color, warm (#EAE0CF) for text
2. Trust indicators use accent/warm colors instead of green
3. Navbar uses primary/secondary gradient background
4. Loader does not appear on fast page loads (<300ms)
5. Navbar mobile menu uses new palette
6. `npm run build` in frontend directory completes without errors
</verification>

<success_criteria>
- Hero section looks professional with clear value proposition (heading, subtitle, stats, CTA)
- All color references in HomePage.jsx use the new palette (primary, secondary, accent, warm)
- All color references in Navbar.jsx use the new palette
- useSmartLoader integrated and working — no loader flicker on fast loads
- App.jsx auth checker uses new palette
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-enhancement/01-02-SUMMARY.md`
</output>
